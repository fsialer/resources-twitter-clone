apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-comments-pvc-dev
  namespace: development
spec:
  volumeName: mongo-comments-pv-dev
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi