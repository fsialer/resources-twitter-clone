apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service-dev
  namespace: development
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users-service-dev
  template:
    metadata:
      labels:
        app: users-service-dev
    spec:
      containers:
        - image: feralexis007/twitter-clone-users-service:dev-0d68b9d
          name: users-service-dev
          ports:
            - containerPort: 8080
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: students-service
                  key: db_host
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: POSTGRES_PASSWORD