apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo6_likes-dev
  namespace: development
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo6_likes-dev
  template:
    metadata:
      labels:
        app: mongo6_likes-dev
    spec:
      containers:
        - image: mongo:6.0
          name: mongo6_likes-dev
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secrets
                  key: MONGO_INITDB_ROOT_USERNAME
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secrets
                  key: MONGO_INITDB_ROOT_PASSWORD
            - name: MONGO_INITDB_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: likes-service-dev
                  key: db_name
          volumeMounts:
            - name: mongo-likes
              mountPath: /data/db
      volumes:
        - name: mongo-likes
          persistentVolumeClaim:
            claimName: mongo-likes-pvc-dev
