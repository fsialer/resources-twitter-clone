apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-posts-pvc-dev
  namespace: development
spec:
  volumeName: mongo-posts-pv-dev
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi