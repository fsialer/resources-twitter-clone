apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-followers-pvc-dev
  namespace: development
spec:
  volumeName: mongo-followers-pv-dev
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi