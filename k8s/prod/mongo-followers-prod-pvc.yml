apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-followers-pvc-prod
  namespace: production
spec:
  volumeName: mongo-followers-pv-prod
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi