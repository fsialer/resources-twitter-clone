apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-comments-pvc-prod
  namespace: production
spec:
  volumeName: mongo-comments-pv-prod
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi