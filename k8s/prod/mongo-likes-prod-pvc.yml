apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-likes-pvc-prod
  namespace: production
spec:
  volumeName: mongo-likes-pv-prod
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi